extends ./layout.jade

block title
  title= title

block content
  header(class="tc pv4 pv5-ns")
    img(src="/img/logo.jpg" class="br3 ba b--black-10 h3 w3" alt="avatar")
    h1(class="f5 f4-ns fw6 black-70")= message
    p
      time(datetime="#{time}")= time
