extends ./layout.jade

block title
  title= title

block content
  header(class="tc pv4 pv5-ns")
    img(src="/img/logo.png" class="br3 ba b--black-10 h3" alt="avatar")
    h1(class="f5 f4-ns fw6 black-70") Endpoints
    ol(class='list pl0')
      each link in links
        li
          a(class='link dim black-60' href=link.url)= link.title
